{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "FrameVerse",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}